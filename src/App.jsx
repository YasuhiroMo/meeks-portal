import { useState, useEffect, useRef, useCallback } from "react";

/* ===== 定数 ===== */
const COMPANY_PASS = "meeks2025";
const ADMIN_PASS = "admin2025";
const PASS_RATE = 80;

/* ===== 共通スタイル ===== */
const S = {
  bg: "#f0f2f5",
  card: { background: "#fff", borderRadius: 16, boxShadow: "0 2px 12px rgba(0,0,0,0.06)", overflow: "hidden" },
  btn: (c) => ({ padding: "10px 20px", borderRadius: 10, border: "none", background: c, color: "#fff", cursor: "pointer", fontSize: 13, fontWeight: 600 }),
  input: { width: "100%", padding: "11px 14px", borderRadius: 10, border: "1.5px solid #ddd", fontSize: 14, outline: "none", boxSizing: "border-box" }
};
const fmt = d => { try { const dt = new Date(d); return `${dt.getMonth()+1}/${dt.getDate()} ${dt.getHours()}:${String(dt.getMinutes()).padStart(2,"0")}`; } catch { return d; } };

/* ===== Persistent Storage ===== */
const stor = {
  get: async (k, sh=true) => { try { return await window.storage?.get(k, sh); } catch { return null; } },
  set: async (k, v, sh=true) => { try { return await window.storage?.set(k, v, sh); } catch { return null; } },
  list: async (p, sh=true) => { try { return await window.storage?.list(p, sh); } catch { return { keys: [] }; } },
  del: async (k, sh=true) => { try { return await window.storage?.delete(k, sh); } catch { return null; } }
};

/* ===== 手順書データ（全13件） ===== */
const MANUALS = [
  { id:"delivery", title:"納入時作業手順書", icon:"📋", tag:"2025年度版 修正版", color:"#1B4F72",
    sections:[
      { title:"出発前5点確認（会社出発前）", content:"【出発前点検事項】指差し呼称で確認\n\nNo.1 アウトリガー格納確認 — 敷板は回収されているか、ロックはされているか。\nNo.2 ブーム格納確認 — ワイヤーの乱巻はないか、正しく格納されているか。\nNo.3 製品荷締め確認 — 落下、荷崩れの可能性はないか。\nNo.4 部材荷締め確認 — 走行中に飛ばされる可能性はないか。\nNo.5 不具合箇所の修正 — 不具合を認識したままの出発は禁止。" },
      { title:"作業手順 No.1〜6", content:"No.1【出発前点検】5点確認を実施。不具合のある状態で出発しない。\nNo.2【現場到着】現場前に停車。監督・業者様に電話連絡。8:00指定は30分以上前の進入禁止。\nNo.3【荷降ろし位置確認】指示内容と乖離がないか確認。危険がある場合は相談。\nNo.4【車両を停車位置に移動】桝・水道・障害物を把握。スタック可能性あれば受注課に連絡。\nNo.5【アウトリガー設置】地盤確認・敷板設置・最大位置まで張出し。U字溝蓋上への設置禁止。\nNo.6【荷解き】荷崩れ確認後、足元の安全を確保。" },
      { title:"作業手順 No.7〜12", content:"No.7【荷降ろし開始】頭上確認（電線・電話線）必須。斜め吊り禁止。風速10m/s以上は作業中止。\nNo.8【荷降ろし終了】製品養生・納品写真撮影・明細書にサイン受領。\nNo.9【ジブ・ブーム格納】頭上確認しながら格納。急旋回禁止。\nNo.10【アウトリガー格納】左右格納・敷板格納・PTO解除。退出前5点確認を実施。\nNo.11【荷締め・出発準備】荷台荷締め・出発前写真撮影。\nNo.12【帰庫】荷台確認・整理・盗難防止装置作動・ハンドルロック。" },
      { title:"退出前5点確認（各現場出発前）", content:"①ブーム格納 — 縮小・旋回・定位置に格納済み\n②アウトリガー格納 — 左右格納・敷板回収・ロック確認\n③ワイヤー乱巻なし — 確認済み\n④フック格納 — 格納位置に固定済み\n⑤PTO解除 — 解除済みであることを確認\n\n呼称：「ブーム格納よし！アウトリガー格納よし！ワイヤー乱巻なしよし！フック格納よし！PTO解除よし！」" }
    ]
  },
  { id:"accident", title:"事故対応手順書", icon:"🚨", tag:"2025年度版 修正版", color:"#C0392B",
    sections:[
      { title:"事故発生時の3原則", content:"① 安全確保（二次災害の防止）\n② 救護・通報（119番・110番）\n③ 会社連絡（配車担当：090-1213-9803）\n\n【絶対禁止事項】\n・現場からの離脱\n・事故報告の隠蔽\n・示談交渉\n・「私が悪い」等の責任を認める発言\n・SNSへの投稿" },
      { title:"電線切断時の対応", content:"1. 絶対に車両から降りない（感電の危険）\n2. 110番に連絡\n3. 配車担当に連絡（090-1213-9803）\n4. 東京電力に連絡（0120-995-007）\n5. 救急・警察の到着まで車内で待機" },
      { title:"緊急連絡先一覧", content:"警察：110（24時間）\n救急・消防：119（24時間）\n配車担当者：090-1213-9803（業務時間内）\n茨城工場受注課：090-6301-8209（8:00〜17:00）\nメークス本社：0297-55-1380\n時間外：0297-55-1380（17:00〜翌8:00・土日祝）\n東京電力：0120-995-007\nNTT：0120-444-113" }
    ]
  },
  { id:"checklist", title:"ミス防止チェックリスト集", icon:"✅", tag:"2025年度版 修正版", color:"#1E8449",
    sections:[
      { title:"出発前チェック", content:"□ 配車表の日付・配送先・時間指定を確認\n□ 袋詰め（伝票）と配車表を照合\n□ 製品と明細表の数量・品番を確認\n□ 荷締め・養生を確認\n□ 出発前5点確認を実施" },
      { title:"現場到着〜納品", content:"□ 配車表の住所と到着した現場が一致\n□ 監督・業者様に挨拶し納品場所を確認\n□ 頭上の電線位置を確認\n□ アウトリガー最大張出し・敷板設置\n□ 明細書にサイン受領\n□ 納品写真を撮影" },
      { title:"退出前チェック", content:"□ ブーム格納\n□ アウトリガー格納\n□ フック格納\n□ ワイヤー乱巻なし\n□ PTO解除\n□ 敷板・玉掛け用具を積み戻し\n□ 忘れ物なし\n□ 現場を汚していないか確認" }
    ]
  },
  { id:"health", title:"健康管理・季節別対策", icon:"💪", tag:"安全教育資料", color:"#2E86C1",
    sections:[
      { title:"基本の健康管理", content:"【睡眠】1日7時間以上。SAS（睡眠時無呼吸症候群）の自覚があれば医師に相談。\n【飲酒】ビール中瓶1本＝分解に約3〜4時間。3本＝約9〜12時間。\n【食事】朝食必須（血糖値低下防止）。昼食は腹八分目（満腹は眠気誘発）。\n【持病・服薬】眠気の副作用がある薬は会社に申告。自己判断の服薬中止は禁止。" },
      { title:"季節別対策", content:"【熱中症予防】WBGT31以上は原則外作業中止。1時間にコップ2〜3杯の水分補給。\n軽度（めまい）→涼しい場所で水分補給。重度（意識障害）→119番。\n\n【冬季対策】スタッドレスタイヤ12月〜3月必須。凍結注意：橋の上・トンネル出入口・日陰。\n\n【台風時】暴風域予想→運行中止。風速15m/s以上→走行危険。冠水道路進入禁止。\n\n【花粉症】主治医に「運転業務」を伝え眠気の少ない薬を処方。市販薬の自己判断使用は危険。" }
    ]
  },
  { id:"driving", title:"走行中の安全", icon:"🚛", tag:"安全教育資料", color:"#6C3483",
    sections:[
      { title:"速度・運転ルール", content:"【速度制限】住宅街20km/h以下。一般道は法定速度厳守。\n【ながら運転】厳禁。スマホ操作は必ず安全な場所に停車してから。\n【あおり運転】挑発に乗らず安全な場所に移動→110番。\n【高速道路故障時】ハザード→路肩停車→三角表示板→車外避難（ガードレール外へ）。" },
      { title:"改善基準告示", content:"【拘束時間】1日：原則13時間以内（上限16時間）。1か月：原則284時間。\n【連続運転】4時間以内に30分以上休憩。\n【勤務間休息】継続11時間以上が基本。" }
    ]
  },
  { id:"kyt", title:"危険予知トレーニング（KYT）", icon:"⚠️", tag:"安全教育資料", color:"#D4AC0D",
    sections:[
      { title:"KYT 4ラウンド法", content:"第1R：危険の洗い出し（どんな危険があるか）\n第2R：危険の絞り込み（最も重要な危険は）\n第3R：対策の立案（あなたならどうする）\n第4R：行動目標の設定（指差し呼称項目を決定）" },
      { title:"KYTシナリオ集", content:"【KYT-1：住宅街でのバック走行】\n場面：住宅街の狭い道路、電柱あり、左に乗用車、右に民家ブロック塀。\n危険：①左車両と接触 ②右ブロック塀にオーバーハング接触◎ ③歩行者の飛び出し◎\n対策：降車して後方確認、誘導者配置\n\n【KYT-2：雨天時の荷降ろし】\n場面：小雨、現場の地面はぬかるみ、スロープあり。\n危険：①スリップして転倒◎ ②アウトリガー沈下◎ ③製品水濡れ\n対策：敷板を多めに使用、足元確認\n\n【KYT-3：電線下でのクレーン作業】\n場面：荷降ろし位置の真上に電線、高さ約6m。\n危険：①ブーム・ワイヤーが電線に接触◎ ②感電\n対策：電線位置を確認し安全な距離を確保、車両位置を変更" }
    ]
  },
  { id:"crane", title:"クレーン操作安全手順", icon:"🏗️", tag:"安全教育資料", color:"#A04000",
    sections:[
      { title:"操作前確認", content:"□ 小型移動式クレーン運転技能講習修了証の携帯\n□ クレーン各部の入念な点検\n□ 過負荷防止装置・巻過防止装置の機能確認\n□ 定格荷重を超える荷物は吊り上げない\n□ アウトリガーを最大位置まで張り出し（片側のみ禁止）\n□ 敷板を必ず設置" },
      { title:"操作中の注意", content:"・吊り荷の下に人を立ち入らせない\n・急激な操作を避け荷振れを最小限に\n・移動しながらの操作禁止\n・複数レバーの同時操作禁止\n・旋回時は頭上確認（電線・電話線）\n・風速10m/s以上は作業中止" }
    ]
  },
  { id:"loading", title:"積込・養生手順", icon:"📦", tag:"安全教育資料", color:"#1A5276",
    sections:[
      { title:"積込の基本", content:"・配送順番を考慮した積込（先に降ろすものを外側に）\n・重心を低く、偏りのないように積載\n・荷締めはラッシングベルト最低2本以上\n・製品間に当て物（角当て）を入れる\n・メーカー毎の養生シート準備\n・後部はみ出し部に赤い布を取り付け" }
    ]
  },
  { id:"site_manner", title:"現場マナー", icon:"🏠", tag:"安全教育資料", color:"#117A65",
    sections:[
      { title:"現場でのルール", content:"・遣り方（やりかた）・水糸・饅頭を絶対に破損しない\n・地縄の内側に製品を置かない\n・防湿シートを汚さない・破損しない\n・近隣住宅の塀・車両・植栽に注意\n・現場を汚した場合は必ず清掃\n・ゴミのポイ捨て厳禁" }
    ]
  },
  { id:"law12", title:"法定12項目ドライバー教育資料", icon:"📚", tag:"国土交通省告示準拠", color:"#34495E",
    sections:[
      { title:"第1項〜第3項", content:"【第1項：事業用自動車の心構え】\n安全最優先。急いでいても速度超過や確認省略をしない。\n\n【第2項：運行の安全確保】\n日常点検・点呼・アルコールチェック（0.00mg/L）・運転日報提出。\n\n【第3項：車両の構造上の特性】\nトラックは内輪差が大きく、オーバーハングあり。制動距離が長い。死角が多い。" },
      { title:"第4項〜第6項", content:"【第4項：正しい積載方法】\n重心を低く偏りなく。ラッシングベルト2本以上。はみ出し部に赤い布。\n\n【第5項：過積載の危険性】\n制動距離増大・横転リスク・行政処分の対象。\n\n【第6項：危険物運搬の留意事項】\n通常業務では危険物運搬なし。事故遭遇時は安全な距離をとり風上に移動。" },
      { title:"第7項〜第12項", content:"【第7項：適切な運行経路】高さ・重量制限を確認。狭い道路には無理に進入しない。\n【第8項：危険予測・緊急時対応】KYT教材・事故対応手順書を参照。\n【第9項：運転適性に応じた安全運転】適性診断で特性を把握。\n【第10項：生理的・心理的要因】過労運転禁止。「だろう」→「かもしれない」運転。\n【第11項：健康管理】睡眠7h以上。飲酒後の運転厳禁。\n【第12項：安全装置】バックカメラ等は補助。必ず目視確認を併用。" }
    ]
  },
  { id:"driver_edu", title:"ドライバー教育チェック表", icon:"📝", tag:"2025年度版", color:"#6C3483",
    sections:[
      { title:"身だしなみ・マナー", content:"□ 作業服は清潔なものを着用\n□ ヘルメット着用、あご紐確実に締める\n□ 安全靴着用・保護手袋着用\n□ お客様・近隣住民への積極的な挨拶\n□ 丁寧な言葉遣い\n□ 時間指定の厳守\n□ 現場・現場付近は禁煙" },
      { title:"クレーン・書類・積込", content:"□ 修了証の携帯\n□ アウトリガー最大張り出し・片側禁止\n□ 風速10m/s以上は作業中止\n□ 配車表・明細表の確認\n□ 配送順を考慮した積込\n□ 荷締め・養生の徹底" },
      { title:"現場作業・点検", content:"□ 到着連絡・荷降ろし位置確認\n□ 明細書にサイン受領\n□ 退出前5点確認\n□ 車両・ユニック日常点検\n□ アルコールチェック\n□ 運転日報の記入・提出" }
    ]
  },
  { id:"customer", title:"顧客満足・マナー", icon:"🤝", tag:"安全教育資料", color:"#B7950B",
    sections:[
      { title:"マナーの基本", content:"・作業服は清潔なものを着用\n・ヘルメット・安全靴・保護手袋を着用\n・お客様・近隣住民へ積極的に挨拶\n・丁寧な言葉遣い\n・時間指定厳守（早すぎてもNG）\n・遅れる場合は事務所へ早めに報告\n・現場・現場付近は禁煙\n・ゴミのポイ捨て厳禁\n・車両の移動を要求されたら速やかに対応" }
    ]
  },
  { id:"mistakes", title:"よくあるミスと防止策", icon:"🔍", tag:"安全教育資料", color:"#7D3C98",
    sections:[
      { title:"ミス事例と対策", content:"【誤配送】到着時に住所・物件名を配車表と照合\n【数量ミス】声に出して1つずつ数える\n【明細書置き忘れ】退出前に書類の仕分けを確認\n【サイン忘れ】退出前チェックリストで確認\n【ブーム格納忘れ】出発前5点確認を毎回実施\n【敷板回収忘れ】退出前チェックリストに含める\n【製品破損】養生・荷締めの徹底、丁寧な操作" }
    ]
  }
];

/* ===== Eラーニングコースデータ（全13コース） ===== */
const COURSES = [
  { id:"delivery_basic", title:"納入基本手順", icon:"🚚", cat:"基本業務", color:"#1B4F72",
    secs:[{ title:"学習内容", body:"出発前5点確認→現場到着→荷降ろし→退出前5点確認の一連の流れを学びます。" }],
    quiz:[
      { q:"出発前5点確認に含まれないものは？", o:["アウトリガー格納","ブーム格納","タイヤ空気圧測定","製品荷締め"], a:2 },
      { q:"8:00指定の場合、何時より前の進入が禁止？", o:["7:00","7:15","7:30","7:45"], a:2 },
      { q:"アウトリガーの張り出しは？", o:["半分でOK","最大位置","状況に応じて","不要な場合あり"], a:1 },
      { q:"退出前5点確認の最初の項目は？", o:["PTO解除","フック格納","ブーム格納","アウトリガー格納"], a:2 },
      { q:"不具合を認識した場合の対応は？", o:["走行中に修理","そのまま出発","出発前に修正","帰社後に報告"], a:2 }
    ]
  },
  { id:"accident_response", title:"事故対応", icon:"🚨", cat:"緊急対応", color:"#C0392B",
    secs:[{ title:"学習内容", body:"事故発生時の3原則と絶対禁止事項、電線切断時の対応を学びます。" }],
    quiz:[
      { q:"事故発生時の3原則の順番は？", o:["通報→安全確保→会社連絡","安全確保→救護通報→会社連絡","会社連絡→安全確保→通報","救護→会社連絡→安全確保"], a:1 },
      { q:"事故時に絶対してはいけないことは？", o:["写真撮影","警察への連絡","示談交渉","救護活動"], a:2 },
      { q:"電線切断時、最初にすべきことは？", o:["車両から降りる","車両から降りない","電線を触る","写真を撮る"], a:1 },
      { q:"配車担当者の電話番号は？", o:["0297-55-1380","090-6301-8209","090-1213-9803","0120-995-007"], a:2 },
      { q:"時間外の連絡先は？", o:["090-1213-9803","0297-55-1380","090-6301-8209","110"], a:1 }
    ]
  },
  { id:"crane_safety", title:"クレーン操作安全", icon:"🏗️", cat:"安全教育", color:"#A04000",
    secs:[{ title:"学習内容", body:"クレーン操作前の確認事項、操作中の注意点、悪天候時の判断を学びます。" }],
    quiz:[
      { q:"クレーン作業中止の風速条件は？", o:["5m/s以上","8m/s以上","10m/s以上","15m/s以上"], a:2 },
      { q:"アウトリガーで禁止されていることは？", o:["最大張り出し","敷板使用","片側のみ使用","水平確認"], a:2 },
      { q:"U字溝蓋の上にアウトリガーを設置してよいか？", o:["よい","条件付きでよい","禁止","管理者判断"], a:2 },
      { q:"吊り荷の下に人が立ち入ることは？", o:["注意すればOK","絶対禁止","誘導者のみOK","短時間ならOK"], a:1 },
      { q:"複数レバーの同時操作は？", o:["効率的で推奨","禁止","熟練者はOK","低速ならOK"], a:1 }
    ]
  },
  { id:"health_mgmt", title:"健康管理・季節対策", icon:"💪", cat:"健康管理", color:"#2E86C1",
    secs:[{ title:"学習内容", body:"睡眠・飲酒・食事の管理、熱中症予防、冬季対策を学びます。" }],
    quiz:[
      { q:"ビール中瓶1本の分解に必要な時間は？", o:["1〜2時間","3〜4時間","5〜6時間","7〜8時間"], a:1 },
      { q:"熱中症で意識障害がある場合の対応は？", o:["水分補給","涼しい場所で休憩","119番通報","自力で帰社"], a:2 },
      { q:"WBGT何度以上で原則外作業中止？", o:["25","28","31","33"], a:2 },
      { q:"冬季のスタッドレスタイヤ装着期間は？", o:["11月〜2月","12月〜3月","1月〜3月","11月〜3月"], a:1 },
      { q:"花粉症の薬について正しいのは？", o:["市販薬でOK","主治医に運転業務を伝えて処方","薬なしで我慢","同僚の薬を借りる"], a:1 }
    ]
  },
  { id:"driving_safety", title:"走行中の安全", icon:"🚛", cat:"安全運転", color:"#6C3483",
    secs:[{ title:"学習内容", body:"速度制限、改善基準告示、緊急時の対応を学びます。" }],
    quiz:[
      { q:"住宅街での速度制限は？", o:["30km/h以下","20km/h以下","40km/h以下","10km/h以下"], a:1 },
      { q:"1日の拘束時間の原則上限は？", o:["10時間","11時間","13時間","16時間"], a:2 },
      { q:"連続運転の上限は？", o:["2時間","3時間","4時間","5時間"], a:2 },
      { q:"あおり運転を受けた場合の対応は？", o:["速度を上げる","急ブレーキ","安全な場所に移動し110番","クラクション"], a:2 },
      { q:"高速道路で故障した場合、最初にすることは？", o:["修理開始","ハザードランプ点灯","110番","三角表示板設置"], a:1 }
    ]
  },
  { id:"kyt_training", title:"危険予知トレーニング", icon:"⚠️", cat:"KYT", color:"#D4AC0D",
    secs:[{ title:"学習内容", body:"KYT 4ラウンド法を使った危険予知の実践方法を学びます。" }],
    quiz:[
      { q:"KYT第1ラウンドの目的は？", o:["対策の立案","危険の洗い出し","行動目標の設定","危険の絞り込み"], a:1 },
      { q:"KYT第4ラウンドで行うことは？", o:["危険の洗い出し","対策の立案","行動目標・指差し呼称","危険の絞り込み"], a:2 },
      { q:"バック走行時に最も注意すべきことは？", o:["速度","後方の安全確認","ハンドル操作","ミラー調整"], a:1 },
      { q:"雨天時のクレーン作業で注意すべきことは？", o:["作業速度","アウトリガー沈下","荷物の色","書類の濡れ"], a:1 },
      { q:"指差し呼称の目的は？", o:["声を出す練習","確認の確実性を高める","周囲に知らせる","マニュアル通りにする"], a:1 }
    ]
  },
  { id:"loading_method", title:"積込・荷締め", icon:"📦", cat:"基本業務", color:"#1A5276",
    secs:[{ title:"学習内容", body:"正しい積込方法、荷締め・養生の手順を学びます。" }],
    quiz:[
      { q:"ラッシングベルトは最低何本必要？", o:["1本","2本","3本","4本"], a:1 },
      { q:"積込で考慮すべき順番は？", o:["重い順","軽い順","配送順","サイズ順"], a:2 },
      { q:"はみ出し部分に必要なものは？", o:["テープ","赤い布","ロープ","シール"], a:1 }
    ]
  },
  { id:"site_rules", title:"現場マナー・ルール", icon:"🏠", cat:"マナー", color:"#117A65",
    secs:[{ title:"学習内容", body:"現場での禁止事項、遣り方・水糸の取り扱いを学びます。" }],
    quiz:[
      { q:"遣り方・水糸を破損した場合は？", o:["黙って直す","無視する","すぐに報告","帰社後に報告"], a:2 },
      { q:"地縄の内側に製品を置いてよいか？", o:["よい","禁止","監督の許可があればOK","小さいものはOK"], a:1 },
      { q:"現場で喫煙してよい場所は？", o:["車内","現場から離れた場所","トイレ付近","禁煙（場所を問わず）"], a:3 }
    ]
  },
  { id:"law12_edu", title:"法定12項目教育", icon:"📚", cat:"法令", color:"#34495E",
    secs:[{ title:"学習内容", body:"国土交通省告示に基づく法定12項目の教育内容を学びます。" }],
    quiz:[
      { q:"アルコールチェックの基準値は？", o:["0.15mg/L以下","0.00mg/L","0.25mg/L以下","0.10mg/L以下"], a:1 },
      { q:"過積載のリスクに含まれないものは？", o:["制動距離増大","横転リスク","燃費向上","行政処分"], a:2 },
      { q:"トラック特有の注意点でないものは？", o:["内輪差","オーバーハング","死角","低重心"], a:3 }
    ]
  },
  { id:"checklist_edu", title:"チェックリストの使い方", icon:"✅", cat:"基本業務", color:"#1E8449",
    secs:[{ title:"学習内容", body:"各種チェックリストの正しい活用方法を学びます。" }],
    quiz:[
      { q:"チェックリストを使うタイミングは？", o:["出発前のみ","作業の各段階で","帰社後のみ","週1回"], a:1 },
      { q:"チェック漏れを防ぐ方法は？", o:["暗記する","上から順に確認","飛ばし読み","まとめてチェック"], a:1 }
    ]
  },
  { id:"driver_check", title:"教育チェック表", icon:"📝", cat:"教育", color:"#6C3483",
    secs:[{ title:"学習内容", body:"ドライバー教育チェック表の各項目と実施方法を学びます。" }],
    quiz:[
      { q:"教育完了後に必要なことは？", o:["口頭報告","受講者署名と日付","写真撮影","テスト受験"], a:1 },
      { q:"アウトリガーで禁止なのは？", o:["最大張り出し","敷板使用","片側のみ使用","水平確認"], a:2 }
    ]
  },
  { id:"customer_service", title:"顧客満足・マナー", icon:"🤝", cat:"顧客満足", color:"#B7950B",
    secs:[{ title:"学習内容", body:"お客様対応のマナーと顧客満足向上のポイントを学びます。" }],
    quiz:[
      { q:"時間指定に対して正しい行動は？", o:["早すぎてもOK","指定時間ちょうどに到着","指定時間厳守（早すぎもNG）","多少遅れても問題なし"], a:2 },
      { q:"現場・現場付近でタバコは？", o:["OK","禁煙","休憩中はOK","人がいなければOK"], a:1 }
    ]
  },
  { id:"mistake_prevent", title:"ミス防止", icon:"🔍", cat:"安全教育", color:"#7D3C98",
    secs:[{ title:"学習内容", body:"よくあるミスの事例と具体的な防止策を学びます。" }],
    quiz:[
      { q:"誤配送を防ぐ方法は？", o:["急いで配送","到着時に住所・物件名を配車表と照合","ナビに任せる","電話確認不要"], a:1 },
      { q:"数量ミスを防ぐ方法は？", o:["目視で確認","声に出して1つずつ数える","感覚で判断","写真で確認"], a:1 },
      { q:"明細書置き忘れを防ぐ方法は？", o:["記憶に頼る","退出前に書類の仕分けを確認","後で取りに戻る","電話で確認"], a:1 }
    ]
  }
];

/* ===== 回答集（管理者用） ===== */
const ANSWER_SHEETS = [
  { title:"事故対応テスト", content:"Q1: 事故発生時の3原則 → ①安全確保→②救護通報→③会社連絡\nQ2: 絶対禁止事項 → 現場離脱・報告隠蔽・示談交渉\nQ3: 配車担当者電話 → 090-1213-9803\nQ4: 電線切断時 → 車両から降りない\nQ5: 時間外連絡先 → 0297-55-1380" },
  { title:"クレーン操作テスト", content:"Q1: 作業中止風速 → 10m/s以上\nQ2: アウトリガー禁止 → 片側のみ使用\nQ3: U字溝蓋上設置 → 禁止\nQ4: 吊り荷下立入 → 絶対禁止\nQ5: 複数レバー同時操作 → 禁止" },
  { title:"健康管理テスト", content:"Q1: ビール中瓶1本分解 → 3〜4時間\nQ2: 意識障害時 → 119番\nQ3: WBGT → 31以上で中止\nQ4: スタッドレス → 12〜3月\nQ5: 花粉症の薬 → 主治医に運転業務を伝える" }
];

/* ===== チャットボット知識ベース ===== */
const KB = `あなたはメークス株式会社 物流事業部のAIアシスタントです。ドライバーや従業員からの質問に正確・簡潔に回答してください。

【緊急連絡先】配車担当者：090-1213-9803／茨城工場受注課：090-6301-8209（8:00〜17:00）／本社：0297-55-1380／時間外：0297-55-1380（自動転送）／東京電力：0120-995-007／NTT：0120-444-113

【出発前5点確認】①アウトリガー格納②ブーム格納③製品荷締め④部材荷締め⑤不具合修正
【退出前5点確認】①ブーム格納②アウトリガー格納③ワイヤー乱巻なし④フック格納⑤PTO解除

【事故3原則】安全確保→救護通報（119・110）→会社連絡（090-1213-9803）。絶対禁止：現場離脱・報告隠蔽・示談交渉・責任認める発言・SNS投稿
【電線切断】車両から降りない→110番→配車担当→東京電力
【アウトリガー】最大張り出し必須。片側禁止。U字溝蓋上禁止。敷板必須。
【クレーン中止】風速10m/s以上
【健康】睡眠7h以上。ビール中瓶1本=3〜4時間。熱中症：WBGT31以上は作業中止。
【改善基準】1日拘束13h以内（上限16h）。月284h。連続運転4h以内に30分休憩。勤務間休息11h以上。
【KYT4R法】①危険出し→②絞り込み→③対策→④行動目標・指差し呼称

回答は日本語で簡潔に。不明な内容は「管理者に確認してください（090-1213-9803）」と案内。`;

const SUGGESTIONS = ["出発前5点確認の内容は？","事故が起きたときどうすれば？","電線に触れた場合の対処法","改善基準告示の拘束時間は？","熱中症の対処法は？","時間外の連絡先は？"];

/* ===== Login ===== */
function Login({ onLogin }) {
  const [empId, setEmpId] = useState("");
  const [name, setName] = useState("");
  const [pw, setPw] = useState("");
  const [err, setErr] = useState("");
  const login = () => {
    if (!empId.trim() || !name.trim()) { setErr("社員番号と氏名を入力してください"); return; }
    if (pw !== COMPANY_PASS) { setErr("パスワードが違います"); return; }
    onLogin({ empId: empId.trim(), name: name.trim() });
  };
  return (
    <div style={{ minHeight:"100vh", background:"linear-gradient(135deg,#0d1b2a,#1b2d4a,#0d1b2a)", display:"flex", alignItems:"center", justifyContent:"center", fontFamily:"'Helvetica Neue',Arial,'Hiragino Sans',sans-serif" }}>
      <div style={{ background:"#fff", borderRadius:20, padding:"48px 36px", width:"100%", maxWidth:400, boxShadow:"0 20px 60px rgba(0,0,0,0.3)" }}>
        <div style={{ textAlign:"center", marginBottom:32 }}>
          <div style={{ fontSize:48, marginBottom:12 }}>🚚</div>
          <h1 style={{ fontSize:20, fontWeight:700, color:"#1a2d4a", marginBottom:6 }}>メークス物流事業部</h1>
          <p style={{ fontSize:13, color:"#888" }}>統合ポータルシステム</p>
        </div>
        <div style={{ display:"flex", flexDirection:"column", gap:14 }}>
          <div><label style={{ fontSize:12, fontWeight:600, color:"#555" }}>社員番号</label><input value={empId} onChange={e=>{setEmpId(e.target.value);setErr("")}} placeholder="例: D001" style={{...S.input, marginTop:4}} /></div>
          <div><label style={{ fontSize:12, fontWeight:600, color:"#555" }}>氏名</label><input value={name} onChange={e=>{setName(e.target.value);setErr("")}} placeholder="例: 山田太郎" style={{...S.input, marginTop:4}} /></div>
          <div><label style={{ fontSize:12, fontWeight:600, color:"#555" }}>パスワード</label><input type="password" value={pw} onChange={e=>{setPw(e.target.value);setErr("")}} onKeyDown={e=>e.key==="Enter"&&login()} placeholder="社内共通パスワード" style={{...S.input, marginTop:4}} /></div>
          {err && <div style={{ background:"#ffeaea", color:"#c0392b", padding:"10px 14px", borderRadius:8, fontSize:13, textAlign:"center" }}>{err}</div>}
          <button onClick={login} style={{ ...S.btn("#1B4F72"), width:"100%", padding:"14px", fontSize:15, marginTop:8 }}>ログイン</button>
        </div>
        <p style={{ textAlign:"center", fontSize:11, color:"#bbb", marginTop:24 }}>※ 社内関係者のみアクセス可能です</p>
      </div>
    </div>
  );
}

/* ===== Header ===== */
function Header({ tab, setTab, user, onLogout }) {
  const tabs = [
    { id:"home", icon:"🏠", label:"ホーム" },
    { id:"manual", icon:"📄", label:"手順書" },
    { id:"elearning", icon:"🎓", label:"Eラーニング" },
    { id:"chat", icon:"🤖", label:"AIチャット" },
    { id:"admin", icon:"🔐", label:"管理者" }
  ];
  return (
    <header style={{ background:"linear-gradient(135deg,#0d1b2a,#1B4F72)", padding:"0", position:"sticky", top:0, zIndex:100, boxShadow:"0 2px 12px rgba(0,0,0,0.15)" }}>
      <div style={{ maxWidth:1200, margin:"0 auto", padding:"12px 20px" }}>
        <div style={{ display:"flex", justifyContent:"space-between", alignItems:"center" }}>
          <div style={{ display:"flex", alignItems:"center", gap:10 }}>
            <span style={{ fontSize:22 }}>🚚</span>
            <div>
              <div style={{ color:"#fff", fontSize:14, fontWeight:700 }}>メークス物流事業部</div>
              <div style={{ color:"#3498DB", fontSize:10 }}>統合ポータル</div>
            </div>
          </div>
          <div style={{ display:"flex", alignItems:"center", gap:12 }}>
            <span style={{ color:"#aaa", fontSize:11 }}>{user?.name}（{user?.empId}）</span>
            <button onClick={onLogout} style={{ padding:"6px 12px", borderRadius:6, border:"1px solid rgba(255,255,255,0.3)", background:"transparent", color:"#fff", fontSize:11, cursor:"pointer" }}>ログアウト</button>
          </div>
        </div>
        <nav style={{ display:"flex", gap:2, marginTop:10, overflowX:"auto" }}>
          {tabs.map(t => (
            <button key={t.id} onClick={() => setTab(t.id)} style={{ background: tab===t.id ? "rgba(255,255,255,0.15)" : "transparent", border:"none", color:"#fff", padding:"10px 18px", cursor:"pointer", fontSize:12, borderRadius:"8px 8px 0 0", opacity: tab===t.id ? 1 : 0.6, fontWeight: tab===t.id ? 700 : 400, borderBottom: tab===t.id ? "2px solid #3498DB" : "2px solid transparent", whiteSpace:"nowrap" }}>{t.icon} {t.label}</button>
          ))}
        </nav>
      </div>
    </header>
  );
}

/* ===== Home ===== */
function Home({ setTab, user }) {
  const cards = [
    { icon:"📄", title:"手順書を確認する", desc:"納入手順・事故対応・チェックリスト等を閲覧", tab:"manual", color:"#1B4F72" },
    { icon:"🎓", title:"Eラーニングを受講する", desc:`全${COURSES.length}コースの安全教育・確認テスト`, tab:"elearning", color:"#1E8449" },
    { icon:"🤖", title:"AIに質問する", desc:"手順書・教育資料の内容をQ&Aで即確認", tab:"chat", color:"#6C3483" }
  ];
  return (
    <div style={{ maxWidth:1000, margin:"40px auto", padding:"0 20px" }}>
      <div style={{ textAlign:"center", marginBottom:36 }}>
        <div style={{ fontSize:42, marginBottom:10 }}>🚚</div>
        <h1 style={{ fontSize:24, fontWeight:700, color:"#1a2d4a", marginBottom:8 }}>物流事業部 統合ポータル</h1>
        <p style={{ color:"#888", fontSize:13 }}>ようこそ、{user?.name}さん</p>
      </div>
      <div style={{ display:"grid", gridTemplateColumns:"repeat(auto-fit,minmax(260px,1fr))", gap:16 }}>
        {cards.map(c => (
          <div key={c.tab} onClick={() => setTab(c.tab)} style={{ ...S.card, padding:"28px 24px", cursor:"pointer", borderTop:`4px solid ${c.color}`, transition:"transform .2s" }} onMouseEnter={e=>e.currentTarget.style.transform="translateY(-4px)"} onMouseLeave={e=>e.currentTarget.style.transform="none"}>
            <div style={{ fontSize:32, marginBottom:12 }}>{c.icon}</div>
            <h3 style={{ fontSize:16, fontWeight:700, color:"#1a2d4a", marginBottom:6 }}>{c.title}</h3>
            <p style={{ fontSize:12, color:"#888" }}>{c.desc}</p>
          </div>
        ))}
      </div>
      <div style={{ ...S.card, marginTop:24, padding:"20px 24px" }}>
        <h3 style={{ fontSize:14, fontWeight:700, color:"#C0392B", marginBottom:10 }}>🚨 緊急連絡先</h3>
        <div style={{ fontSize:13, color:"#333", lineHeight:2 }}>配車担当：090-1213-9803 ／ 受注課：090-6301-8209 ／ 本社：0297-55-1380 ／ 時間外：0297-55-1380</div>
      </div>
    </div>
  );
}

/* ===== ManualViewer ===== */
function ManualViewer() {
  const [sel, setSel] = useState(null);
  const [sec, setSec] = useState(0);
  if (!sel) return (
    <div style={{ maxWidth:1000, margin:"0 auto", padding:"24px 20px" }}>
      <h2 style={{ fontSize:18, fontWeight:700, color:"#1a2d4a", marginBottom:16 }}>📄 手順書一覧</h2>
      <div style={{ display:"grid", gridTemplateColumns:"repeat(auto-fill,minmax(220px,1fr))", gap:12 }}>
        {MANUALS.map(m => (
          <div key={m.id} onClick={() => { setSel(m); setSec(0); }} style={{ ...S.card, padding:"20px", cursor:"pointer", borderLeft:`4px solid ${m.color}` }}>
            <div style={{ display:"flex", justifyContent:"space-between" }}>
              <span style={{ fontSize:24 }}>{m.icon}</span>
              <span style={{ fontSize:9, color:"#888", background:"#f0f0f0", padding:"2px 8px", borderRadius:10, height:"fit-content" }}>{m.tag}</span>
            </div>
            <div style={{ fontSize:13, fontWeight:700, color:"#1a2d4a", marginTop:8 }}>{m.title}</div>
            <div style={{ fontSize:11, color:"#aaa", marginTop:4 }}>{m.sections.length}セクション</div>
          </div>
        ))}
      </div>
    </div>
  );
  return (
    <div style={{ maxWidth:900, margin:"0 auto", padding:"24px 20px" }}>
      <button onClick={() => setSel(null)} style={{ background:"none", border:"none", color:"#2E86C1", cursor:"pointer", fontSize:13, marginBottom:12 }}>← 一覧に戻る</button>
      <div style={S.card}>
        <div style={{ padding:"20px 24px", borderBottom:"1px solid #e0e0e0", background:`linear-gradient(135deg,${sel.color}11,${sel.color}05)` }}>
          <span style={{ fontSize:28 }}>{sel.icon}</span>
          <h2 style={{ fontSize:17, fontWeight:700, color:"#1a2d4a", marginTop:6 }}>{sel.title}</h2>
        </div>
        <div style={{ display:"flex", borderBottom:"1px solid #eee", overflowX:"auto" }}>
          {sel.sections.map((s,i) => (
            <button key={i} onClick={() => setSec(i)} style={{ background:"none", border:"none", padding:"12px 16px", cursor:"pointer", fontSize:12, fontWeight: sec===i?700:400, color: sec===i?sel.color:"#666", borderBottom: sec===i?`2px solid ${sel.color}`:"2px solid transparent", whiteSpace:"nowrap" }}>{s.title}</button>
          ))}
        </div>
        <div style={{ padding:"24px", whiteSpace:"pre-line", fontSize:14, lineHeight:2, color:"#333" }}>{sel.sections[sec].content}</div>
      </div>
    </div>
  );
}

/* ===== ELearning ===== */
function ELearning({ user }) {
  const [course, setCourse] = useState(null);
  const [ans, setAns] = useState({});
  const [sub, setSub] = useState(false);
  const [score, setScore] = useState(null);
  const [progress, setProgress] = useState({});

  useEffect(() => { (async () => { try { const r = await stor.get(`prog:${user.empId}`); if (r?.value) setProgress(JSON.parse(r.value)); } catch {} })(); }, [user.empId]);

  const submit = async () => {
    const correct = course.quiz.reduce((n, q, i) => n + (ans[i] === q.a ? 1 : 0), 0);
    const pct = Math.round((correct / course.quiz.length) * 100);
    const passed = pct >= PASS_RATE;
    setScore({ correct, total: course.quiz.length, pct, passed });
    setSub(true);
    const rec = { empId: user.empId, name: user.name, courseId: course.id, courseName: course.title, score: correct, totalQ: course.quiz.length, pct, passed, date: new Date().toISOString() };
    await stor.set(`res:${user.empId}:${course.id}:${Date.now()}`, JSON.stringify(rec));
    const np = { ...progress, [course.id]: { completed: passed, last: rec.date, pct } };
    setProgress(np);
    await stor.set(`prog:${user.empId}`, JSON.stringify(np));
  };

  const reset = () => { setCourse(null); setAns({}); setSub(false); setScore(null); };

  if (!course) return (
    <div style={{ maxWidth:1000, margin:"0 auto", padding:"24px 20px" }}>
      <h2 style={{ fontSize:18, fontWeight:700, color:"#1a2d4a", marginBottom:16 }}>🎓 Eラーニング</h2>
      <p style={{ fontSize:12, color:"#888", marginBottom:20 }}>合格ライン：{PASS_RATE}%以上 ／ 全{COURSES.length}コース</p>
      <div style={{ display:"grid", gridTemplateColumns:"repeat(auto-fill,minmax(200px,1fr))", gap:12 }}>
        {COURSES.map(c => {
          const p = progress[c.id];
          return (
            <div key={c.id} onClick={() => setCourse(c)} style={{ ...S.card, padding:"16px", cursor:"pointer", position:"relative" }}>
              <div style={{ position:"absolute", top:0, left:0, right:0, height:3, background:c.color }} />
              <div style={{ display:"flex", justifyContent:"space-between", alignItems:"start" }}>
                <span style={{ fontSize:22 }}>{c.icon}</span>
                {p?.completed && <span style={{ background:"#27AE60", color:"#fff", fontSize:10, padding:"2px 8px", borderRadius:10, fontWeight:600 }}>修了</span>}
              </div>
              <div style={{ fontSize:12, fontWeight:700, color:"#1a2d4a", marginTop:6 }}>{c.title}</div>
              <div style={{ fontSize:10, color:"#888", marginTop:3 }}>{c.cat} • {c.quiz.length}問</div>
              {p?.last && <div style={{ fontSize:10, color:"#aaa", marginTop:4 }}>最終: {fmt(p.last)}</div>}
            </div>
          );
        })}
      </div>
    </div>
  );

  return (
    <div style={{ maxWidth:800, margin:"0 auto", padding:"24px 20px" }}>
      <button onClick={reset} style={{ background:"none", border:"none", color:"#2E86C1", cursor:"pointer", fontSize:13, marginBottom:12 }}>← コース一覧に戻る</button>
      <div style={S.card}>
        <div style={{ padding:"20px 24px", borderBottom:"1px solid #e0e0e0" }}>
          <span style={{ fontSize:28 }}>{course.icon}</span>
          <h2 style={{ fontSize:17, fontWeight:700, color:"#1a2d4a", marginTop:6 }}>{course.title}</h2>
          <p style={{ fontSize:12, color:"#888", marginTop:4 }}>合格ライン: {PASS_RATE}% ／ {course.quiz.length}問</p>
        </div>
        {sub && score && (
          <div style={{ padding:"24px", textAlign:"center", background: score.passed ? "#d4edda" : "#f8d7da" }}>
            <div style={{ fontSize:36 }}>{score.passed ? "🎉" : "📝"}</div>
            <div style={{ fontSize:18, fontWeight:700, color: score.passed ? "#155724" : "#721c24", marginTop:8 }}>{score.passed ? "合格！" : "不合格"}</div>
            <div style={{ fontSize:14, marginTop:6, color:"#333" }}>{score.correct}/{score.total} 正解（{score.pct}%）</div>
            {!score.passed && <button onClick={() => { setAns({}); setSub(false); setScore(null); }} style={{ ...S.btn("#E67E22"), marginTop:12 }}>再受験する</button>}
          </div>
        )}
        <div style={{ padding:"24px" }}>
          {course.quiz.map((q, qi) => (
            <div key={qi} style={{ marginBottom:24, padding:"16px", background: sub ? (ans[qi] === q.a ? "#d4edda" : "#f8d7da") : "#f8f9fa", borderRadius:12 }}>
              <div style={{ fontSize:13, fontWeight:700, color:"#1a2d4a", marginBottom:10 }}>Q{qi+1}. {q.q}</div>
              {q.o.map((opt, oi) => (
                <div key={oi} onClick={() => !sub && setAns({...ans, [qi]:oi})} style={{ padding:"10px 14px", marginBottom:4, borderRadius:8, border: `1.5px solid ${ans[qi]===oi ? course.color : "#e0e0e0"}`, background: sub ? (oi===q.a ? "#c3e6cb" : ans[qi]===oi ? "#f5c6cb" : "#fff") : (ans[qi]===oi ? `${course.color}15` : "#fff"), cursor: sub ? "default" : "pointer", fontSize:13, display:"flex", alignItems:"center", gap:8 }}>
                  <span style={{ fontSize:10 }}>{ans[qi]===oi ? "●" : "○"}</span>{opt}
                </div>
              ))}
            </div>
          ))}
          {!sub && <button onClick={submit} disabled={Object.keys(ans).length !== course.quiz.length} style={{ ...S.btn(Object.keys(ans).length === course.quiz.length ? course.color : "#ccc"), width:"100%", padding:"14px" }}>テストを提出する</button>}
        </div>
      </div>
    </div>
  );
}

/* ===== Chatbot（★ /api/chat 経由） ===== */
function Chatbot() {
  const [msgs, setMsgs] = useState([{ role:"assistant", content:"こんにちは！物流部AIアシスタントです。\n手順書・緊急連絡先・KYT・法令など何でも聞いてください。" }]);
  const [input, setInput] = useState("");
  const [loading, setLoading] = useState(false);
  const btm = useRef(null);
  useEffect(() => { btm.current?.scrollIntoView({ behavior:"smooth" }); }, [msgs]);

  const send = async (text) => {
    const q = text || input.trim();
    if (!q || loading) return;
    setInput("");
    const nm = [...msgs, { role:"user", content:q }];
    setMsgs(nm);
    setLoading(true);
    try {
      const res = await fetch("/api/chat", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          system: KB,
          messages: nm.filter(m => m.role !== "system").map(m => ({ role: m.role, content: m.content }))
        })
      });
      const data = await res.json();
      const reply = data.content?.find(c => c.type === "text")?.text || "申し訳ありません。もう一度お試しください。";
      setMsgs(p => [...p, { role:"assistant", content:reply }]);
    } catch (e) {
      setMsgs(p => [...p, { role:"assistant", content:"エラーが発生しました。しばらく経ってからお試しください。" }]);
    }
    setLoading(false);
  };

  return (
    <div style={{ maxWidth:800, margin:"0 auto", padding:"20px", height:"calc(100vh - 130px)", display:"flex", flexDirection:"column" }}>
      <div style={{ ...S.card, flex:1, display:"flex", flexDirection:"column" }}>
        <div style={{ padding:"14px 20px", borderBottom:"1px solid #e0e0e0", display:"flex", alignItems:"center", gap:10 }}>
          <span style={{ fontSize:22 }}>🤖</span>
          <div>
            <div style={{ fontSize:14, fontWeight:700, color:"#1a2d4a" }}>物流部 AIアシスタント</div>
            <div style={{ fontSize:10, color:"#27AE60" }}>● 手順書・KYT・法令を学習済み</div>
          </div>
        </div>
        <div style={{ flex:1, overflowY:"auto", padding:"16px 20px", display:"flex", flexDirection:"column", gap:10 }}>
          {msgs.map((m, i) => (
            <div key={i} style={{ display:"flex", justifyContent: m.role==="user" ? "flex-end" : "flex-start" }}>
              <div style={{ maxWidth:"80%", padding:"10px 14px", borderRadius:12, fontSize:13, lineHeight:1.7, whiteSpace:"pre-line", background: m.role==="user" ? "#1B4F72" : "#f0f2f5", color: m.role==="user" ? "#fff" : "#333" }}>{m.content}</div>
            </div>
          ))}
          {loading && <div style={{ display:"flex" }}><div style={{ padding:"10px 14px", borderRadius:12, background:"#f0f2f5", fontSize:13, color:"#888" }}>考えています...</div></div>}
          <div ref={btm} />
        </div>
        <div style={{ padding:"8px 12px", borderTop:"1px solid #eee", display:"flex", gap:6, overflowX:"auto" }}>
          {SUGGESTIONS.map(s => <button key={s} onClick={() => send(s)} style={{ padding:"6px 12px", borderRadius:16, border:"1px solid #2E86C1", background:"#fff", color:"#2E86C1", fontSize:11, cursor:"pointer", whiteSpace:"nowrap" }}>{s}</button>)}
        </div>
        <div style={{ padding:"12px", borderTop:"1px solid #e0e0e0", display:"flex", gap:8 }}>
          <input value={input} onChange={e => setInput(e.target.value)} onKeyDown={e => e.key==="Enter" && !e.shiftKey && send()} placeholder="質問を入力してください..." style={{ flex:1, padding:"11px 14px", borderRadius:12, border:"1.5px solid #ddd", fontSize:13, outline:"none" }} />
          <button onClick={() => send()} disabled={!input.trim() || loading} style={{ ...S.btn(input.trim() && !loading ? "#1B4F72" : "#ccc"), padding:"11px 22px" }}>送信</button>
        </div>
      </div>
    </div>
  );
}

/* ===== AdminPanel ===== */
function AdminPanel() {
  const [auth, setAuth] = useState(false);
  const [pw, setPw] = useState("");
  const [results, setResults] = useState([]);
  const [view, setView] = useState("dashboard");
  const [ansSec, setAnsSec] = useState(0);

  const login = () => { if (pw === ADMIN_PASS) setAuth(true); else alert("パスワードが違います"); };

  const loadResults = useCallback(async () => {
    try {
      const r = await stor.list("res:");
      const all = [];
      if (r?.keys) { for (const k of r.keys) { try { const d = await stor.get(k); if (d?.value) all.push(JSON.parse(d.value)); } catch {} } }
      all.sort((a, b) => new Date(b.date) - new Date(a.date));
      setResults(all);
    } catch {}
  }, []);

  useEffect(() => { if (auth) loadResults(); }, [auth, loadResults]);

  if (!auth) return (
    <div style={{ maxWidth:400, margin:"60px auto", padding:20 }}>
      <div style={{ ...S.card, padding:"32px 28px" }}>
        <div style={{ textAlign:"center", marginBottom:20 }}>
          <div style={{ fontSize:36 }}>🔐</div>
          <h2 style={{ fontSize:17, fontWeight:700, color:"#1a2d4a", marginTop:8 }}>管理者ログイン</h2>
        </div>
        <input type="password" value={pw} onChange={e => setPw(e.target.value)} onKeyDown={e => e.key==="Enter" && login()} placeholder="管理者パスワード" style={{ ...S.input, marginBottom:16 }} />
        <button onClick={login} style={{ ...S.btn("#C0392B"), width:"100%" }}>ログイン</button>
      </div>
    </div>
  );

  const uniq = [...new Map(results.map(r => [r.empId, r])).values()];
  const passC = results.filter(r => r.passed).length;

  return (
    <div style={{ maxWidth:1100, margin:"0 auto", padding:"24px 20px" }}>
      <div style={{ display:"flex", justifyContent:"space-between", alignItems:"center", marginBottom:20 }}>
        <h2 style={{ fontSize:17, fontWeight:700, color:"#1a2d4a" }}>🔐 管理者ダッシュボード</h2>
        <button onClick={loadResults} style={{ ...S.btn("#2471A3"), padding:"8px 14px", fontSize:12 }}>🔄 更新</button>
      </div>
      <div style={{ display:"flex", gap:8, marginBottom:20, flexWrap:"wrap" }}>
        {[{ k:"dashboard", l:"📊 概要" }, { k:"results", l:"📋 受験履歴" }, { k:"answers", l:"🔐 回答集" }, { k:"guide", l:"📝 変更ガイド" }].map(v => (
          <button key={v.k} onClick={() => setView(v.k)} style={{ padding:"8px 16px", borderRadius:8, border:"none", cursor:"pointer", fontSize:12, fontWeight: view===v.k ? 700 : 400, background: view===v.k ? "#1a2d4a" : "#e8e8e8", color: view===v.k ? "#fff" : "#555" }}>{v.l}</button>
        ))}
      </div>

      {view === "dashboard" && (
        <div>
          <div style={{ display:"grid", gridTemplateColumns:"repeat(auto-fit,minmax(150px,1fr))", gap:12, marginBottom:20 }}>
            {[["📊","総受験回数",results.length],["👤","受験者数",uniq.length+"名"],["✅","合格数",passC],["📈","合格率",results.length ? Math.round(passC/results.length*100)+"%" : "-%"]].map(([ic,l,v]) => (
              <div key={l} style={{ ...S.card, padding:16 }}>
                <div style={{ fontSize:20, marginBottom:4 }}>{ic}</div>
                <div style={{ fontSize:11, color:"#888" }}>{l}</div>
                <div style={{ fontSize:20, fontWeight:700, color:"#1a2d4a" }}>{v}</div>
              </div>
            ))}
          </div>
        </div>
      )}

      {view === "results" && (
        <div style={S.card}>
          <div style={{ padding:"14px 20px", borderBottom:"1px solid #e0e0e0", fontSize:14, fontWeight:700, color:"#1a2d4a" }}>全受験履歴</div>
          {results.length === 0 ? <div style={{ padding:30, textAlign:"center", color:"#aaa", fontSize:13 }}>まだ受験データがありません</div> : (
            <div style={{ overflowX:"auto" }}>
              <table style={{ width:"100%", borderCollapse:"collapse", fontSize:12 }}>
                <thead><tr style={{ background:"#f8f9fa" }}>
                  {["日時","社員番号","氏名","コース","スコア","結果"].map(h => <th key={h} style={{ padding:"10px 14px", textAlign:"left", fontWeight:600, color:"#555" }}>{h}</th>)}
                </tr></thead>
                <tbody>{results.map((r,i) => (
                  <tr key={i} style={{ borderBottom:"1px solid #f0f0f0" }}>
                    <td style={{ padding:"10px 14px", whiteSpace:"nowrap" }}>{fmt(r.date)}</td>
                    <td style={{ padding:"10px 14px" }}>{r.empId}</td>
                    <td style={{ padding:"10px 14px" }}>{r.name}</td>
                    <td style={{ padding:"10px 14px" }}>{r.courseName || r.courseId}</td>
                    <td style={{ padding:"10px 14px" }}>{r.score}/{r.totalQ}（{r.pct}%）</td>
                    <td style={{ padding:"10px 14px" }}><span style={{ background: r.passed ? "#d4edda" : "#f8d7da", color: r.passed ? "#155724" : "#721c24", padding:"2px 8px", borderRadius:10, fontSize:11, fontWeight:600 }}>{r.passed ? "合格" : "不合格"}</span></td>
                  </tr>
                ))}</tbody>
              </table>
            </div>
          )}
        </div>
      )}

      {view === "answers" && (
        <div style={S.card}>
          <div style={{ padding:"14px 20px", borderBottom:"1px solid #e0e0e0", fontSize:14, fontWeight:700, color:"#1a2d4a" }}>📝 回答集【管理者用】</div>
          <div style={{ background:"#fff3cd", padding:"10px 20px", fontSize:12, color:"#856404", borderBottom:"1px solid #ffc107" }}>⚠ 本資料は管理者・教育担当者用です。ドライバーには配布しないでください。</div>
          <div style={{ display:"flex", borderBottom:"1px solid #eee", overflowX:"auto" }}>
            {ANSWER_SHEETS.map((s,i) => <button key={i} onClick={() => setAnsSec(i)} style={{ background:"none", border:"none", padding:"12px 16px", cursor:"pointer", fontSize:12, fontWeight: ansSec===i ? 700 : 400, color: ansSec===i ? "#922B21" : "#666", borderBottom: ansSec===i ? "2px solid #922B21" : "2px solid transparent", whiteSpace:"nowrap" }}>{s.title}</button>)}
          </div>
          <div style={{ padding:"24px", whiteSpace:"pre-line", fontSize:13, lineHeight:2, color:"#333" }}>{ANSWER_SHEETS[ansSec].content}</div>
        </div>
      )}

      {view === "guide" && (
        <div style={S.card}>
          <div style={{ padding:"14px 20px", borderBottom:"1px solid #e0e0e0", fontSize:14, fontWeight:700, color:"#1a2d4a" }}>📝 コンテンツ変更ガイド</div>
          <div style={{ padding:"24px", fontSize:13, lineHeight:2, color:"#333" }}>
            <p style={{ fontWeight:700, color:"#C0392B", marginBottom:12 }}>手順書・Eラーニングの内容変更が必要な場合：</p>
            <p>Claudeに指示することで変更できます。</p>
            <div style={{ background:"#f8f9fa", borderRadius:8, padding:"14px 18px", marginTop:12 }}>
              変更依頼の例：<br/>• 「納入手順書の○○を追加して」<br/>• 「テスト問題を△△に変更して」<br/>• 「AIチャットの知識ベースに□□を追加」<br/>• 「法令改正に伴い数値を更新」
            </div>
          </div>
        </div>
      )}
    </div>
  );
}

/* ===== App（メイン） ===== */
export default function App() {
  const [tab, setTab] = useState("home");
  const [user, setUser] = useState(null);

  useEffect(() => {
    try { const s = sessionStorage.getItem("meeks_user"); if (s) setUser(JSON.parse(s)); } catch {}
  }, []);

  const handleLogin = (u) => { setUser(u); sessionStorage.setItem("meeks_user", JSON.stringify(u)); };
  const handleLogout = () => { setUser(null); setTab("home"); sessionStorage.removeItem("meeks_user"); };

  if (!user) return <Login onLogin={handleLogin} />;

  return (
    <div style={{ minHeight:"100vh", background:S.bg, fontFamily:"'Helvetica Neue',Arial,'Hiragino Sans',sans-serif" }}>
      <Header tab={tab} setTab={t => { setTab(t); window.scrollTo(0,0); }} user={user} onLogout={handleLogout} />
      <main style={{ minHeight:"calc(100vh - 80px)" }}>
        {tab === "home" && <Home setTab={setTab} user={user} />}
        {tab === "manual" && <ManualViewer />}
        {tab === "elearning" && <ELearning user={user} />}
        {tab === "chat" && <Chatbot />}
        {tab === "admin" && <AdminPanel />}
      </main>
    </div>
  );
}